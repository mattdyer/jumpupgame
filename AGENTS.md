# Project Instructions

- The project is a git repo, so you can use that to look at file history.
- Write unit tests for all code
- Write end to end tests for all features
- Run `npm run test` after code changes
- Run `npm run e2e` after new features are added
- You can commit changes but do not push or pull